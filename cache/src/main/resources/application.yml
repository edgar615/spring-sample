server:
    port: 8888


spring:
    application:
        name: cache
    cache:
        cache-names: testCache, cache1, cache2
        caffeine:
            spec: maximumSize=500,expireAfterAccess=600s
