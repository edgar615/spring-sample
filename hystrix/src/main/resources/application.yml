server:
    port: 9000

spring:
    application:
        name: @project.artifactId@

info:
    name: @project.artifactId@
    build:
        artifact:   @project.artifactId@
        name: @project.artifactId@
        version: @project.version@
        description: 这是一个测试用例

management:
    endpoints:
        web:
            exposure:
                include: '*'
            base-path: /manage